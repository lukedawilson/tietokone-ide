{
  "name": "tietokone-ide",
  "version": "0.1.0",
  "scripts": {
    "build": "vite build",
    "heroku-postbuild": "npm install && npm run build"
  },
  "dependencies": {
    "vite": "^6.0.6"
  },
  "engines": {
    "node": "23.x",
    "npm": "10.x"
  }
}